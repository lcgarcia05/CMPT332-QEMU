Shell is unfinished, but can process single command inputs.
    - Can handle stdin redirection
    - Bug on stdout redirection where it includes the file name to its output
    - Duplicate char is not implemented

How to run:
    - To start the wrdsh program, execute the makefile and then ./wrdsh
    